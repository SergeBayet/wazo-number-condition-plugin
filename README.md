# Wazo Number Condition Plugin

Experimental Wazo plugin to route incoming calls based on caller number patterns.

Goal:

- match incoming caller numbers using regex
- route matching calls to a Wazo destination
- expose the feature in the Wazo UI under Call Management

## Status

Work in progress.